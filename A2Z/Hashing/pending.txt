#include <bits/stdc++.h>
using namespace std;
void max(int a[],int n){
    int x=a[0];
    for(int i=0; i<n; i++){
        if(a[i]>x){
            x=a[i];
        }
    }
    cout<<x;
}
void min(int a[],int n){
    int x=a[0];
    for(int i=0;i<n;i++){
        if(a[i]<x){
            x=a[i];
        }
    }
    cout<<x;
}

int main() {
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    
    // Pre-compute
    unordered_map<int,int> mpp;
    for(int i=0;i<n;i++){
        mpp[a[i]]++;
    }
    
    int q;
    cin>>q;
    int e[q];
    for(int i=0;i<q;i++){
        cin>>e[i];
    }
    for(int i=0;i<q;i++){
        cout<<mpp[e[i]]<<endl;
        
    }
    for(auto it:m){
        for
        cout
    }
    cout<<max(mpp,q)<<" "<<min(mpp,q);
    return 0;
}